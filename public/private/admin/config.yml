backend:
  name: github
  repo: samjsdev/decap
  branch: main
  # This tells Decap CMS where your OAuth proxy (Cloudflare Pages Function) lives
  base_url: https://decap-b03.pages.dev

# IMPORTANT: Keep this commented out for production. 
# Only uncomment this line when you are testing locally with 'npx decap-server'
# local_backend: true

media_folder: "public/uploads"
public_folder: "/uploads"

publish_mode: editorial_workflow

collections:
  - name: "posts"
    label: "Blog Posts"
    folder: "content/posts"
    create: true
    slug: "{{slug}}"
    extension: "md"
    format: "frontmatter"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "date", format: "YYYY-MM-DD" }
      - { label: "Excerpt", name: "excerpt", widget: "text" }
      - { label: "Author", name: "author", widget: "string", default: "Harbor Smile Team" }
      - { label: "Body", name: "body", widget: "markdown" }